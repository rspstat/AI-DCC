## 2026년 9월 19일 2차 멘토링 데이 서기

- 라벨 데이터가 생략된 부분도 존재할건데, 그 부분이 많을 수도 있다. 주어진 라벨 안에서 테스트를 하는거고 다른팀들도 같은 라벨로 하는것이다. 라벨 수정은 불가능하다. 왜 성능이 이정도 밖에 안되는지 조금 더 논리적으로 서술을 해주면 긍정적으로 작용할 것이다.
- 라벨을 바꾼다기보단, 지금 있는 라벨로 모델 개발을 진행하는게 좋을 것이다.
- 트레인드 데이터셋에서 전처리를 한다기 보단, 명시된 애들로만 학습을 하냐?
    - 문제를 보면 기타 통증이나 그런건 전부 전처리가 되서, 그것까지 전부 포함시켜서 하는 것이 좋을 것이다. validation 데이터셋은 우리가 명시적으론 모르는 데이터셋이지만, 관찰하면 알 수 있을 것이다. 변진호님이 말씀하신 전략으로 진행하는게 유리할지, 전체로 진행해서 유리할지는 직접 판단해서 하는것이 좋다. 실질적으로 성능을 비교를 해보고, 어떤 결과가 성능이 나오는지 논리적으로 설명하고, PT에 잘 서술하는게 좋다.
- 9개 클래스에 대해 Multi lavel로 분류를 하는건데, 다른 라벨로 살려놓고 진행하면 성능이 좋을 것 같지는 않을 것이다. 라벨을 다 예측하는 방식은 그다지 좋지 않을 것이다. validation 데이터셋은 9개 출력으로 제한을 두고 있다. validation 데이터셋이 없으면 문제가 발생할 수 있다. 9개만 따지는 미션이다.
- 환자의 텍스트만 보고 라벨을 만드는 것이 아니라, 처치가 끝난 후에 라벨을 만드는 것이다. 예시 : 텍스트에서 열이 있다고 하더라도, 라벨이 고열로 안된 데이터셋이 존재할 것이다. 비교해보는 것이 좋다.
- 학습 기법 중에 유사한 데이터셋에 라벨이 다를 때 사용하는 기법들이 존재해서 찾아보고 하는 것도 추천한다.
- 지금 validation 셋이 관측 가능하니까, training 셋으로 학습을 시키고, 성능 테스트는 validation 데이터셋을 사용하는게 성능적으로 유리할 것이다.
- 데이터 전치리 등이 멀티 GPU가 아니더라도 돌아갈 수 있게만 하면 상관 없다. Magic Number 들을 -1 등이 있을텐데, 결국 재연이 되도록 구현해야 한다. 
- 파라미터 optimization function 뭘 사용하는가?     
    - 성능이 쪼금 바뀌는데 드라마틱하게 바뀌진 않는다.
    - 랜덤성이 존재해서 성능이 바뀌긴하는데 엄청 큰 차이는 존재하지 않는다.
    - 이 프로젝트 자체가 다른 팀과 정량적인 성능 평가를 하는 것이기 때문에, 내가 더이상 고칠게 없을 때 배치 사이즈를 조절해보는 것도 좋다.
- 조정하기 전에 왜 안되는지를 봐야한다. 클래스 불균형이 발생하는 구간이 존재할 것이다. 
    - False-weight 에 더 줬는데 성능이 안좋아진다는 것은 더 학습할게 없다는 것이고, 한계점이 존재한다는 것이다. 노이즈가 많이 껴있는지 텍스트 자체가 이상한지 체크하는 것이 좋다. 임베딩 공간에서 어떤 차이가 있는지 보는 것이 좋다. 4차원으로 생성하는 것도 좋은 방법이다. 
    - 고열, 복통 등의 클래스를 유심히 살펴보는 것이 좋다.
    - 그 다음에 False-weight를 유사한 임베딩 공간에 있다하면 조정이 필요하다. 
    - 벡터 임베딩을 하는 모델들도 바꾸는 것도 좋은 방법이다.
    - 오심이라는 것들이 다른 클래스가 구분될 수 있도록 모양을 띄게 만드는 모델이 무엇이 있는지 list-up 을 각각 해보면 좋다. False-weight는 차후 선택이고, 모델한테 편향을 쎄게 주는 것이다.
    - 인간이 설정한 편향이 맞다면 모델이 잘 작동할 수 있겠지만, Multi-lavel에서 잘 작동할 확률은 낮다.
- 미션 1에서 극단적으로 길지 않으면 제약이 없고, 상식선에서 추론 가능한 선이면 괜찮다.
- 미션 1에서 다른 Architecture 를 더 시도해보는 것이 좋다.
- PItch-Shift는 현재 테스트와 맞지 않아 aggmentation은 성별이 맞지 않으면 쓰는게 좋은데, 아직 그런 문제가 발생하는 것이 아니기에, 사용할 필요 없다. F1-Score 도 살펴보고 불균형이 나오면 Data Aggementation을 사용하는 것도 괜찮지만, 문제를 정의하고 어느 부분에서 안풀리는지 판단을 하고 Aggmentation을 진행하는 것을 정하면 된다.
- 미션 2에서 Binary Classfication인데 90퍼 언저리면 학습이 잘못된 것 같다. 이 부분은 다시 한 번 체크를 해봐야 한다. 미션 1에서 똑같은 데이터를 쓰고 똑같은 Input을 쓰고 하는 것인데, 미션 1과 2의 차이가 너무 심하다. 이상하다. 체크 해봐라.
- 0.5라해서 50%인 줄 알았으나, 90%로 다시 인지하고 아직은 말할 수 없다. 계속 수정해보는 것이 좋다.
- 모델의 lower bound 를 잡고 더 올려야 하는데, 오해하는 것 중에 하나가 Model Architecture 를 바꾼다고 좋아지지 않는다. 학습하는 파이프라인이나 전처리 이 부분에서 살펴보는 것이 좋다. 거기서도 문제가 없다 하면은 Model Parameter 수정 해보고 하는 것이 좋다.
- 앙상블을 시도하면 개선될 확률이 높긴 하다. 앙상블 기법 자체가 모델의 아키텍처가 다르면 다를수록 앙상블 자체 효과는 좋아져서 도움이 될 것이다. 10개에서 결과가 비슷하게 나왔다. 나같으면 마지막에 Combination을 해서 앙상블을 계속 시도하여 성능을 높이지 않을까 싶다. 물론 마지막 단계에서만...
- 어떤 탐색에서 어떤 근거가 있었느냐?
    - 보통 하이퍼파라미터를 튜닝을 할때는(설정), EDA를 통해서 어떤 판단 근거로 하는지 필요함.
    - 데이터가 특정 시점에 따라 중앙값 설정 필요하다. 너희들이 알아서 판단해라.
- layer를 제거하면 통으로 다시 학습시켜야 한다. MLP라고 하면 5층 쌓여 있다. 완전히 다 학습이 되어 있다하면, 그리고 각 층 별로 역할이 있는데, 시점 중에 하나를 제거하게 해서 완전한 예측을 하라는 것은 자신있게 학습틀 시켜야 할 필요가 있다. 레이어가 큰 모델일수록 제거하기 힘들다. 모델 구조는 바꾸지 말고 가장 끝단을 바꾸는 것도 괜찮다.
- 굉장히 실험적은 부분은 말할 수 없다. 과적합 부분은 계속 실험을 해보는 것이 좋다.
- 그 모델 자체가 인간의 언어나 텍스트를 잘 이해하고 있다 싶으면, 굳이 많은 레이어를 건들 필요는 없다.
- 4 에포크가 돌았을 때 성능이 멈춘거면 모델 자체의 성능이 거기까지인 것이다. 데이터셋 자체가 어려운 것이 아니기 때문에 그 에포크에서 멈춘거면 모델 자체가 거기까지 학습하고 결과를 내놓았다는 것이다.
- 데이터의 다른 Field 사용하는 것은 제한된다.
- Attention Map을 한 번 하나의 샘플에 대해 찍어보면 알 수 있다. (MFCC)
- thresh hold는 다르게 하면 안되고, 고정된 상태에서 진행해주면 된다. 수정 금지.
- 오심을 어떻게 구분할 수 있는지, 어떤 Cluster를 잡을 수 있는지 아까 말한대로 thresh hold를 기본값으로 돌리더라도 결과가 잘 나오도록 화이팅해라.
